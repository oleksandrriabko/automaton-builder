export const START_NODE_ID: string = "1";

export const ACTIVATE_DRAW_EDGE_KEY_NAME: string = "Shift";
export const ACTIVATE_DELETE_ELEMENTS_KEY_NAME: string = "Delete";

export enum EMPTY_EDGE_LABEL_SIGN {
  LAMBDA = "λ",
  EPSILON = "ε",
}

export enum AUTOMATA_TYPE {
  FINITE = "finite",
  PUSHDOWN = "pushdown",
}

export enum USER_ROLES {
  PROFESSOR = "professor",
  STUDENT = "student",
}

export const DEFAULT_ERROR_API_RESPONSE_MESSAGE = "Error! Something went wrong";

export const PUSHDOWN_BOTTOM_SYMBOL = "Z";
